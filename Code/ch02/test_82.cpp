#include <iostream>
#include <assert.h>

using namespace std;

int main(int argc, char *argv[])
{
    // A:x = INT_MIN
    // B:正确，无论是否溢出
    // C：正确，见P66旁注，-x和~x+1得到的结果完全一样
    // D：正确
    // E：正确

    return 0;
}